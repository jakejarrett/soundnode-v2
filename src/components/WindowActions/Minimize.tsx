import styled from "styled-components";
import { common } from './common';

export const Minimize = styled.div({
    ...common,
    backgroundColor: 'var(--minimize-color)',

    // '&:hover': {
    // 	color: '
    // }

})