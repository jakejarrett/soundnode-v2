import styled from "styled-components";
import { common } from './common';

export const Maximize = styled.div({
    ...common,
    backgroundColor: 'var(--maximize-color)',

    // '&:hover': {
    // 	color: '
    // }

})